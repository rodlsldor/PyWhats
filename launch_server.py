from server import Server

if __name__ == "__main__":
    host = '127.0.0.1'  # Remplacez par l'adresse IP appropriée
    port = 12345  # Remplacez par le port approprié
    server = Server(host, port)